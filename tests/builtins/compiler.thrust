/*
 * Compile-time builtins: compilerVersion, debugBuild.
 */

fn main() s32 @public {
    if stringLength(compilerVersion()) == 0 { return 1; }
    if debugBuild() != false { return 2; }

    return 0;
}