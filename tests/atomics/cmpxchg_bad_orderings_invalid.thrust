fn main() s32 @public {
    var atomicRelax counter: u32 = 10;
    var expected: u32 = 10;

    var swapped: bool = atomicCompareAndSwap(counter, expected, 20, atomicSync, atomicDrop);

    if swapped { return 1; }

    return 0;
}