import std::io;

fn main() s32 @public {
    io::lockStream(io::stdout);
    if io::tryLockStream(io::stdout) != 0 { return 1; }
    io::unlockStream(io::stdout);
    io::unlockStream(io::stdout);

    return 0;
}