/*
 * Sanitizer build with pointer dereferences.
 */

directive "--sanitizer=address";
directive "--disable-warnings=W0020";

fn main() s32 @public {
    var value: s32 = 42;
    var pointer: ptr[s32] = ref value;

    if (deref pointer) != 42 { return 1; }

    return 0;
}