/*
 * Sanitizer propagation with -fsanitize=thread on a trivial program.
 */

directive "--sanitizer=thread";
directive "--disable-warnings=W0020";

fn main() s32 @public {
    var value: s32 = 0;
    value = value + 1;

    if value != 1 { return 1; }

    return 0;
}