/*
 * Sanitizer build with memory sanitizer on a trivial program.
 */

directive "--sanitizer=memory";
directive "--disable-warnings=W0020";

fn main() s32 @public {
    var a: s32 = 1;
    var b: s32 = 2;

    if a + b != 3 { return 1; }

    return 0;
}