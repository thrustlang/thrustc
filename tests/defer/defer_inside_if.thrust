static mut trace: s32 = 0;

fn add(value: s32) void {
    trace = trace * 10 + value;
}

fn run() void {
    var keep: s32 = 9;

    if keep > 0 {
        defer add(1);
    }

    defer add(2);
}

fn main() s32 @public {
    run();
    if trace != 12 { return 1; }
    return 0;
}