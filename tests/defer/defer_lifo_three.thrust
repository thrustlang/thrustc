static mut trace: s32 = 0;

fn push(value: s32) void {
    trace = trace * 10 + value;
}

fn run() void {
    defer push(1);
    defer push(2);
    defer push(3);
}

fn main() s32 @public {
    run();
    if trace != 321 { return 1; }
    return 0;
}
