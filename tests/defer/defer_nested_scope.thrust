static mut trace: s32 = 0;

fn add(value: s32) void {
    trace = trace + value;
}

fn run() void {
    {
        defer add(1);
    }
    defer add(2);
}

fn main() s32 @public {
    run();
    if trace != 3 { return 1; }
    return 0;
}