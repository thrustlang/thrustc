/*
 * Verifies property->name RValue: loads the field value (deref) after the GEP.
 */

struct Inner {
    a: s32,
    b: s32,
}

fn main() s32 @public {
    var v := new Inner { a: 7, b: 3 };

    var sum: s32 = v->a + v->b;

    if sum != 10 { return 1; }

    return 0;
}
