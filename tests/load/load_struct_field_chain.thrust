/*
 * load through a struct field pointer.
 */

struct Node {
    next: ptr[s32],
}

fn main() s32 @public {
    var a: s32 = 7;
    var n := new Node { next: ref a };

    var loaded: ptr[s32] = load n.next as ptr[s32];

    if (deref loaded) != 7 { return 1; }

    return 0;
}