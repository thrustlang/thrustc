/*
 * Conditional standard library imports: @if / @else selection with value check.
 */

@if(isLinux()) import std::testmod only { std_val };
@else import std::other only { std_val };

fn main() s32 @public {
    if std_val() != 7 { return 1; }

    return 0;
}