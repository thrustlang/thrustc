/*
 * Invalid: compile-time conditional with no active branch
 *
 * When every condition is false and there is no '@else' branch, the
 * compilation must fail.
 */

fn main() s32 @public {
    @if(false) {
        var x: s32 = 1;
    }

    return 0;
}