fn main() s32 @public {
    var count: s32 = 0;

    @if(sizeOf(u64) == 8) {
        count += 1;
    } @else {
        count += 100;
    }

    @if(sizeOf(u32) == 8) {
        count += 100;
    } @else {
        count += 2;
    }

    if count != 3 { return 1; }

    return 0;
}