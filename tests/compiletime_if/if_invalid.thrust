/*
 * Invalid: compile-time condition that is not a constant boolean
 *
 * The condition must fold to a compile-time constant; a runtime value is
 * rejected.
 */

fn main() s32 @public {
    var runtime: s32 = 5;

    @if(runtime == 5) {
        var x: s32 = 1;
    } @else {
        var y: s32 = 2;
    }

    return 0;
}